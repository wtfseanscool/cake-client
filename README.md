# cake-client updater repo

This repository hosts updater metadata for Cake Client.

- `info.json` provides the latest version string.
- `update/update.json` provides the updater manifest consumed by the client.
- GitHub Releases provide platform binaries.

Expected Windows release asset names:

- `DDNet-win64.exe`
- `DDNet-Server-win64.exe`
